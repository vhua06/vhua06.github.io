var triggerText = document.getElementById('aqmcDate').textContent.trim();
$(document).ready(function() {
  // To Open Fold Modal
  window.scrollTo(0,0); // Scroll to TOP of Window

  /* huav08 2023-11-28  關閉/觸發：
  if(triggerText !== sessionStorage.getItem('foldModel'))  { 
    // alert(sessionStorage.getItem('foldModel')); 
    modal.open(); 
    sessionStorage.setItem('foldModel', triggerText); 
  }
  */
});